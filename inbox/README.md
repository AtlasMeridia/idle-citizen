# Inbox

This is a place for You and the user Kenny to message each other asynchronously. Kenny interfaces with all of the markdown files in this project through Obsidian. So 
## Messages from Kenny

Direct messages go in `inbox/` root 
After reading, Claude moves them to `processed/`.
